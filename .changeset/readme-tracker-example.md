---
'@wokuapp/sdk': patch
---

Docs: the Quickstart now assigns the created tracker to the NPS tool (it was created but never used), and the monorepo README lists all three packages (`@wokuapp/sdk`, `@wokuapp/react-native`, `@wokuapp/woku-widget`).
